node_modules/
dist/
dist-server/
.env
# NOT .env.development — it is dev-only config with no secrets, and the dev server needs it.
.env.local
.DS_Store

# Python bytecode from running the art generators
__pycache__/
*.pyc

# Server room snapshots — live game state, machine-local, never source.
.rooms.json
.rooms.json.tmp
