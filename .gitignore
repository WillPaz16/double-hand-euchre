node_modules/
dist/
.env
# NOT .env.development — it is dev-only config with no secrets, and the dev server needs it.
.env.local
.DS_Store

# Python bytecode from running the art generators
__pycache__/
*.pyc

# Local Durable Object storage from `wrangler dev` — live game state, never source.
.wrangler/

# Generated by `wrangler types` (run by `npm run typecheck:worker`) — 15k lines, derived from wrangler.jsonc.
worker-configuration.d.ts
